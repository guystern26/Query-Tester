# Copy this file to .env.local and fill in your local Splunk address.
# .env.local is gitignored and will NOT be committed.

# Splunk Web address for Vite dev proxy (default: http://localhost:8000)
VITE_SPLUNK_DEV_HOST=http://localhost:8000
